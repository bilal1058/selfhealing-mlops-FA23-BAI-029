groups:
  - name: sentiment-drift-rules
    rules:
      - alert: ModelConfidenceDrift
        expr: prediction_confidence_score < 0.607
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Model confidence drift detected"
          description: "Prediction confidence dropped below assigned threshold 0.607 for more than 1 minute."
